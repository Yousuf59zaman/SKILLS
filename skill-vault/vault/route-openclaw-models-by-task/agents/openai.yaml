interface:
  display_name: "Route OpenClaw Models by Task"
  short_description: "Route browser actions to Luna safely"
  default_prompt: "Use $route-openclaw-models-by-task to route explicit browser actions to Luna while keeping ordinary tasks on OmniRoute."
